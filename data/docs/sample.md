# Sample Document

This is a placeholder document for the retrieval step to eventually
index and search over. Replace this folder's contents with real
domain documents once we move from scaffold to working retrieval
(embedding + vector search) in a later session.
